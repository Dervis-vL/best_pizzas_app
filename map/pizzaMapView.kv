#:import MapView kivy_garden.mapview.MapView

<PizzaMapView>:
    lat: 41.0722451
    lon: 14.3188612
    zoom: 10
    on_zoom:
        self.zoom = 6 if self.zoom < 6 else self.zoom
    on_lat:
        self.start_getting_pizza_in_fov()
    on_lon:
        self.start_getting_pizza_in_fov()